import WellSwapPlatform from '../components/WellSwapComplete';
import ContractTest from '../components/ContractTest';

export default function Home() {
  return (
    <div>
      {/* 기존 WellSwap 플랫폼 */}
      <WellSwapPlatform />
      
      {/* 컨트랙트 테스트 섹션 추가 */}
      <section className="py-12 bg-gray-50">
        <div className="container mx-auto px-4">
          <div className="text-center mb-8">
            <h2 className="text-3xl font-bold text-gray-900 mb-4">
              🔗 Smart Contract Integration Test
            </h2>
            <p className="text-gray-600 max-w-2xl mx-auto">
              Test the connection to our deployed smart contract on BSC Testnet. 
              This verifies that all blockchain functions are working correctly.
            </p>
          </div>
          <ContractTest />
        </div>
      </section>
    </div>
  );
}
